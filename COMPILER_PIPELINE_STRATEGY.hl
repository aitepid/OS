// COMPILER_PIPELINE_STRATEGY.hl -- 编译管线策略（同步版）
//
// 当前策略摘要：
//   - 继续以 scripts/hl-bootstrap-build-test.ps1 作为构建/测试主入口
//   - 保留 scripts/hl-compile-pipeline.ps1 + scripts/rebuild-image.ps1 作为当前落地路径
//   - 不再用旧口径描述为“编译管线完全断裂、只能手写机器码”
//
// 当前真实状态：
//   1. hl-bootstrap 是源码级主入口
//   2. hl-compile-pipeline.ps1 负责当前编译流程
//   3. rebuild-image.ps1 负责当前镜像重建
//   4. kernel.bin 已接入镜像链
//   5. iteration 109 后未解析重定位已降到 1
//
// 下一阶段目标：
//   - TCP 回环已闭环，heval 已证明内核可自举
//   - 稳定 kernel.bin 接入能力
//   - 为 118+ DNS 实测 / kmod 热补丁提供基础路径

fn pipeline_strategy() {
    print("编译管线策略（同步版）");
    print("主入口: hl-bootstrap-build-test.ps1");
    print("当前路径: compile-pipeline + rebuild-image + kernel.bin");
    print("当前状态: unresolved reloc = 1");
}

pipeline_strategy();
