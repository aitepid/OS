// hl-lint.hl -- Compatibility lint entry (pure H-L)
// Canonical lint: bare-kernel/hl/lint.hl

fn run_lint() {
    print("HicOS Compatibility Lint Entry");
    print("Use canonical script: bare-kernel/hl/lint.hl");
    print("Run: hl-bootstrap bare-kernel/hl/lint.hl");
    return 0;
}

run_lint();
