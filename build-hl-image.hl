// build-hl-image.hl -- Compatibility image build entry (pure H-L)
// Canonical build flow: hl-bootstrap -> bare-kernel/hl/build.hl

fn build_hl_image() {
    print("HicOS Image Build Entry");
    print("Use canonical script: bare-kernel/hl/build.hl");
    print("Run: hl-bootstrap bare-kernel/hl/build.hl");
    return 0;
}

build_hl_image();
