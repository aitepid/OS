# HicOS UI 设计策划案

## 1. 文档定位

本策划案用于为 `HicOS` 制定统一的 UI/UX 演进方向，覆盖：
- 裸机启动阶段的视觉体验
- VGA 文本模式界面
- VESA 图形桌面
- 窗口系统、终端、文件管理器、设置中心等核心应用
- 输入设备（PS/2 / USB 键盘、鼠标）与交互模型

目标不是一次性做成“复杂桌面系统”，而是为现有 `vga_console.hl`、`vesa.hl`、`wm.hl`、`usb_kbd.hl`、`kernel_entry.hl` 提供一套可逐迭代落地的 UI 设计基线。

---

## 2. 当前现状评估

结合当前仓库状态，HicOS 已具备以下 UI 基础：

### 2.1 已有能力
- `vga_console.hl`
  - 80×25 文本模式
  - 滚屏、退格、颜色属性、硬件光标
- `rebuild-image.ps1`
  - 启动阶段 VGA 文本回显
  - Shell 提示符与键盘输入可同步显示到 VGA
- `vesa.hl`
  - 线性帧缓冲基础绘制能力
- `wm.hl`
  - 基础窗口管理器雏形
  - 标题栏、边框、鼠标拖动、聚焦等原型能力
- `kernel_entry.hl`
  - 图形命令验证入口，如 `gfxtest`、`wm`、`gterm`
- `usb_kbd.hl`
  - 现代裸机 USB HID 键盘输入能力

### 2.2 主要缺口
- 缺少统一视觉语言，当前颜色、边框、排版规则分散
- 文本模式 UI 与图形模式 UI 风格割裂
- 窗口、控件、图标、状态栏、菜单尚未形成体系
- 图形终端、文件管理器、设置中心没有统一信息架构
- 鼠标、键盘、焦点、快捷键、通知等交互规范尚未制度化
- 缺少“安装器 UI”与“桌面 UI”的连续体验设计

---

## 3. 总体设计目标

### 3.1 产品目标
为 HicOS 建立一套“从引导到桌面”的完整界面体系，使系统具备：
- 可识别的视觉品牌
- 统一的操作逻辑
- 对裸机环境友好的低资源 UI
- 文本模式与图形模式之间可平滑切换的体验

### 3.2 设计原则
1. **轻量优先**
   - 先文字、后图标；先纯色块、后复杂贴图；先结构、后装饰。
2. **裸机优先**
   - 所有 UI 必须适应无 GPU 加速、低分辨率、无字体库、低内存环境。
3. **一致性优先**
   - 统一颜色、边距、标题栏、按钮、输入反馈、错误提示风格。
4. **可回退**
   - 图形失败时，功能必须可回退到 VGA 文本界面。
5. **键盘优先，鼠标增强**
   - 所有关键功能都可仅用键盘完成；鼠标提供效率增强。
6. **自举可实现**
   - 设计必须能被当前 H-L 内核逐步实现，不依赖复杂外部资源。

---

## 4. UI 分层架构

建议将 HicOS UI 分为四层：

### Layer 0: Boot UI
职责：开机、硬件检测、安装前引导反馈。

包含：
- 启动 Banner
- 初始化阶段进度输出
- 错误/降级提示
- 安装器入口提示

建议风格：
- 黑底 + 品牌色高亮
- 重要状态用颜色区分：
  - 成功：绿色
  - 警告：黄色
  - 错误：红色
  - 标题：青色/蓝色

### Layer 1: Text UI
职责：VGA 文本模式下的系统交互。

包含：
- Shell 提示符
- 文本安装器
- 文本菜单
- 文本状态页

建议风格：
- 单线框布局
- 固定宽度文本表格
- 高亮选中行
- 统一命令帮助格式

### Layer 2: Graphical Core UI
职责：VESA 图形桌面基础设施。

包含：
- 桌面背景
- 顶栏 / 底栏
- 窗口管理器
- 鼠标指针
- 图形终端

### Layer 3: System Apps UI
职责：核心系统应用与管理面板。

包含：
- 文件管理器
- 设置中心
- 任务管理器
- 磁盘安装器
- 包管理器
- 日志查看器

---

## 5. 视觉语言方案

## 5.1 品牌关键词
- 几何
- 工程感
- 极简
- 稳定
- 裸机原生

## 5.2 推荐视觉风格
建议采用：
**“低像素密度下的工程极简主义”**

特征：
- 大块纯色
- 低装饰边框
- 清晰对比度
- 8×16 / 8×8 位图字形
- 少量强调色

## 5.3 配色系统

### 基础色板
- 背景主色：`#0F172A`
- 桌面色：`#1E293B`
- 面板色：`#334155`
- 边框色：`#475569`
- 主强调色：`#3B82F6`
- 成功色：`#22C55E`
- 警告色：`#F59E0B`
- 错误色：`#EF4444`
- 前景主色：`#E2E8F0`
- 次级文字：`#94A3B8`

### VGA 对应色建议
由于 VGA 颜色有限，建议映射为：
- 标题：亮青 / 亮蓝
- 普通文字：亮灰
- 成功：亮绿
- 警告：黄
- 错误：亮红
- 选中项：黑底亮白或蓝底白字

## 5.4 字体策略
- 第一阶段：继续使用 8×16 位图字体
- 第二阶段：补齐 8×8 小字号用于状态栏/列表
- 第三阶段：扩展图标字形区（如文件夹、磁盘、网络、齿轮）

---

## 6. 交互设计规范

## 6.1 输入模型
### 键盘
- `Tab`: 焦点切换
- `Enter`: 确认/执行
- `Esc`: 返回/关闭
- `↑ ↓ ← →`: 导航
- `F1`: 帮助
- `F2`: 重命名/编辑
- `F5`: 刷新
- `Alt+Tab`: 窗口切换
- `Ctrl+Alt+T`: 打开终端

### 鼠标
- 左键：选择 / 聚焦 / 拖动
- 双击：打开
- 右键：上下文菜单
- 滚轮：滚动列表 / 终端历史

## 6.2 焦点规则
- 同时只允许一个主焦点窗口
- 焦点窗口标题栏高亮
- 当前控件必须有明显边框或底色变化
- 键盘输入优先发往焦点窗口

## 6.3 反馈规则
- 点击有视觉反馈
- 长操作必须显示进度或忙碌状态
- 危险操作必须二次确认
- 错误提示需包含：问题 + 当前影响 + 下一步建议

---

## 7. 信息架构规划

## 7.1 桌面主界面
建议采用经典三段式布局：

### 顶栏
显示：
- 系统名称 `HicOS`
- 当前用户
- 时间
- 网络/磁盘/电源状态图标

### 桌面区
显示：
- 应用窗口
- 安装器窗口
- 文件管理器
- 图形终端

### 底栏 / 启动栏
显示：
- 应用启动按钮
- 当前任务列表
- 通知入口

## 7.2 核心应用清单
第一批建议实现：
1. `Terminal`
2. `Installer`
3. `Files`
4. `Settings`
5. `System Monitor`
6. `Package Manager`

---

## 8. 关键界面策划

## 8.1 启动界面
目标：让用户一眼判断系统是否正常启动。

布局建议：
- 顶部：`HicOS 6.x`
- 中部：初始化阶段列表
- 底部：快捷提示（例如进入安装器、进入 shell）

状态示例：
- `[ok] Serial`
- `[ok] VGA`
- `[ok] ATA PIO`
- `[ok] USB Keyboard`
- `[warn] Network unavailable`

## 8.2 安装器界面
安装器是“完全裸机安装”的核心 UI，应分为两种模式：

### 文本安装器
适用于：
- VGA 文本模式
- 调试环境
- 显卡初始化失败场景

步骤建议：
1. 目标磁盘检测
2. 容量校验
3. 风险确认
4. 写入当前镜像
5. 校验签名
6. 完成页

### 图形安装器
适用于：
- VESA 图形模式
- 面向最终用户展示

页面建议：
- 欢迎页
- 磁盘选择页
- 安装摘要页
- 进度页
- 完成页

## 8.3 图形终端
目标：成为桌面环境中的核心系统工具。

设计建议：
- 黑底浅字或深蓝底浅字
- 支持滚动历史
- 支持命令高亮
- 支持固定提示符样式
- 支持窗口化与全屏切换

## 8.4 文件管理器
设计建议：
- 左侧：卷/目录树
- 右侧：文件列表
- 底部：状态信息（大小、类型、修改时间）
- 顶部：当前位置、刷新、返回、打开终端

## 8.5 设置中心
模块建议：
- 显示
- 输入设备
- 网络
- 声音
- 存储
- 用户与权限
- 系统信息

---

## 9. 控件系统规划

建议最先实现的基础控件：
- Label
- Button
- Input
- List
- ScrollBar
- CheckBox
- RadioBox
- ProgressBar
- WindowFrame
- MenuBar
- StatusBar
- Dialog

### 控件设计要求
- 所有控件必须支持键盘焦点
- 必须支持低分辨率绘制
- 必须支持纯软件重绘
- 必须支持最小 invalidation / dirty rect 更新

---

## 10. 技术实现映射

## 10.1 现有模块可承接内容
- `vga_console.hl`
  - 文本终端、文本安装器、恢复模式
- `vesa.hl`
  - 基础图形绘制 API
- `wm.hl`
  - 窗口管理、桌面、鼠标交互
- `usb_kbd.hl`
  - USB 键盘输入
- `kernel_entry.hl`
  - 启动 UI、命令入口、图形测试入口

## 10.2 建议新增模块
- `ui_theme.hl`：颜色、间距、字体、图标定义
- `ui_core.hl`：控件树、重绘、事件分发
- `ui_text.hl`：高级文本布局、裁剪、对齐
- `ui_input.hl`：键盘/鼠标统一输入抽象
- `ui_desktop.hl`：桌面层、任务栏、通知区
- `ui_installer.hl`：图形安装器
- `ui_terminal.hl`：图形终端
- `ui_files.hl`：文件管理器
- `ui_settings.hl`：设置中心

---

## 11. 分阶段实施路线

## Phase 1：统一视觉基线
目标：建立可复用样式系统。

输出：
- 颜色系统
- 文本模式 UI 规范
- 图形模式 UI 规范
- 标题栏/边框/按钮统一风格

建议迭代：
- 提炼 `vga_console.hl` 与 `wm.hl` 颜色常量
- 建立 `ui_theme.hl`

## Phase 2：文本安装器优化
目标：让裸机安装在 VGA 文本模式下达到可交付体验。

输出：
- 分步安装流程
- 明确的风险提示与确认页
- 进度条 / 百分比显示
- 成功/失败总结页

## Phase 3：图形桌面基础
目标：让 `wm.hl` 从“演示型窗口绘制”升级为“可交互桌面核心”。

输出：
- 桌面背景
- 顶栏/底栏
- 焦点管理
- 拖拽、关闭、最小化
- 鼠标指针规范

## Phase 4：图形终端
目标：形成真正可用的图形控制台。

输出：
- 图形终端窗口
- 历史滚动
- 焦点输入
- 命令结果渲染

## Phase 5：图形安装器
目标：把当前“完整裸机安装”能力包装成终端用户可理解的安装流程。

输出：
- 磁盘选择页
- 安装摘要页
- 进度页
- 完成页

## Phase 6：桌面系统应用
目标：构成最小可用桌面环境。

输出：
- 文件管理器
- 设置中心
- 系统监控
- 包管理器 UI

---

## 12. 里程碑定义

### M1：可用文本界面
标准：
- 启动日志视觉统一
- 文本安装器流程完整
- Shell 帮助与状态页可读性提升

### M2：可用图形桌面
标准：
- 有桌面、任务栏、窗口焦点、基本鼠标操作
- 图形终端可用

### M3：可用图形安装器
标准：
- 用户可在图形界面完成整盘安装
- 安装过程有进度和错误处理

### M4：最小日用桌面
标准：
- 文件、终端、设置、系统信息均可用
- 形成 HicOS 自有 UI 语言

---

## 13. 风险与规避

## 13.1 性能风险
风险：纯软件绘制导致全屏刷新过慢。

规避：
- 优先 dirty rect 重绘
- 控件局部刷新
- 减少阴影、渐变、透明效果

## 13.2 输入复杂度风险
风险：USB/PS2/鼠标/键盘事件模型分裂。

规避：
- 建立统一输入抽象层
- 键盘优先，鼠标逐步增强

## 13.3 字体与多语言风险
风险：位图字体不足以支撑复杂文本。

规避：
- 第一阶段仅保证英文/数字/基础符号
- 第二阶段再扩展字符集

## 13.4 裸机兼容性风险
风险：不同显卡/分辨率/输入设备路径差异大。

规避：
- 保留 VGA 文本回退路径
- 图形失败时自动切换文本模式

---

## 14. 验收标准

本策划案最终验收以以下结果为准：
- ✅ 有统一 UI 主题规范 → `ui_theme.hl`
- ✅ 有明确的启动、安装、桌面三段体验设计 → Boot UI / VGA / VESA 三层
- ✅ 有可落地到现有模块的实施路线 → Phase 1-6 全部完成
- ✅ 有从文本模式过渡到图形模式的清晰方案 → VGA fallback + VESA desktop
- ✅ 有明确的技术拆分与迭代里程碑 → 迭代 125-130

---

## 15. 推荐的下一步实施顺序

建议下一步按这个顺序推进：
1. `ui_theme.hl`：统一颜色/边框/标题栏规范
2. `installer.hl`：补全文本安装器的交互页面与进度显示
3. `wm.hl`：增加任务栏、焦点、最小化、关闭按钮逻辑
4. `ui_terminal.hl`：图形终端正式化
5. `ui_installer.hl`：图形安装器
6. `ui_files.hl`：文件管理器
7. `ui_settings.hl`：设置中心

---

## 16. 一句话定位

**HicOS 的 UI 应该是：在裸机约束下，兼具工程感、稳定性与可扩展性的轻量级原生桌面系统。**
