fn make_rows() { return [[10, 20], [30, 40]]; }
let rows = make_rows();
push(rows[0], 99);
print("push_nested=" + to_string(rows[0][2]));
print("call_nested_add=" + to_string(make_rows()[0][1] + 1));
