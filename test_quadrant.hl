// test_quadrant.hl -- Phase 0 验证: quadrant 扁平化
// 期望输出: 7, 30, hello

quadrant math {
    fn add(a, b) { return a + b; }
    fn mul(a, b) { return a * b; }
}

quadrant greet {
    fn say(name) { return "hello"; }
}

print(math.add(3, 4));
print(math.mul(5, 6));
print(greet.say("world"));
