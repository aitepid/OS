// test-runner.hl -- Compatibility test entry (pure H-L)
// Canonical tests: bare-kernel/hl/test-runner.hl

fn run_tests() {
    print("HicOS Compatibility Test Runner");
    print("Use canonical script: bare-kernel/hl/test-runner.hl");
    print("Run: hl-bootstrap bare-kernel/hl/test-runner.hl");
    return 0;
}

run_tests();
